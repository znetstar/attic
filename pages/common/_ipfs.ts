
import {create as createIPFS, Options as IPFSOptions, IPFS as IPFSClass } from 'ipfs-core';

// export const IPFS: IPFSClass = (global as any).ipfs = ((global as any).ipfs || createIPFS()) as IPFSClass;
// export default IPFS;
